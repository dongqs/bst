#include <iostream>
using namespace std;

#include "bst.h"

int main()
{
    Node* node = new Node(5);

    // Node* result = NULL;
    // result = node->search(3);

    // node->insert(3);
    // node->insert(7);
    // node->insert(2);

    // delete node .....

    cout << node->val << endl;
    return 0;
}
